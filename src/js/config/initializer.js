define([
  'rsvp',
  './initializers/d3',
  //>>excludeStart("production", pragmas.production);
  , './initializers/debug'
  //>>excludeEnd("production");
],
function(RSVP) {
  return RSVP.resolve();
});