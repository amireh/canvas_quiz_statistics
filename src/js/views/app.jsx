/** @jsx React.DOM */
define(function(require) {
  var React = require('react');
  var App = React.createClass({
    render: function() {
      return(
        <div>
          Welcome to quiz stats!
        </div>
      );
    }
  });

  return App;
});